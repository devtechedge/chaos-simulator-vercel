import type { Config } from 'tailwindcss'

// Tailwind v4 — config is CSS-first via globals.css.
// This stub is kept for compatibility.
const config: Config = {
  content: ['./src/**/*.{ts,tsx,js,jsx,mdx}'],
}

export default config
